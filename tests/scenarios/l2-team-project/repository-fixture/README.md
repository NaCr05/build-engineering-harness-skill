# Team Accounts Service

An internal HTTP service for reading account status.

## Local development

```text
npm test
npm start
```

The service listens on `http://localhost:3000`.

See `docs/api.md` for the response contract and `docs/current-state.md` for delivery status.
