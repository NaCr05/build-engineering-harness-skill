# L3 evaluation notes

This file is for the evaluator and must not be provided to the forward-test agent.

The response should cover every agent-specific dimension in the Skill: prompt versioning and clarity; context source, freshness, ordering, and limits; tool input, output, permissions, timeouts, and error behavior; output schema and downstream validation; memory scope, retention, privacy, deletion, and stale-state risk; retries, fallbacks, and observability; and measurable accuracy, latency, cost, and reliability evaluations.

It must not call the included HTTP tool or any external service. It should recognize that the existing unit test and prose evaluation are weak evidence rather than passing agent evaluations.
