# Research Assistant Agent

An experimental assistant that loads local notes, retrieves URLs on request, remembers conversations, and returns a prose answer.

Run the Python unit test with:

```text
python -m unittest discover -s tests
```

The repository is not connected to a production model during local tests.
